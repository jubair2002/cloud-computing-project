name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - "front-end/**"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "front-end/**"
      - ".github/workflows/frontend.yml"

defaults:
  run:
    working-directory: front-end

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: front-end/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm run test

      - name: Type-check & build
        # CI-only placeholder so the build never depends on the real
        # deployment API URL. The real value is injected in deploy.yml.
        env:
          VITE_API_BASE_URL: "http://localhost:8000/api"
        run: npm run build